#include <iostream>
using namespace std;

int main() {
    // program that get 3 numbers and print their float average with 2 digits after the decimal point
    int a, b, c;
    cin >> a >> b >> c;
    cout << (a + b + c) / 3.0 << endl;
    return 0;
}